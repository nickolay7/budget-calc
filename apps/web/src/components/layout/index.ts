/**
 * Компоненты компоновки приложения.
 *
 * Экспортирует AppShell (основная оболочка с сайдбаром и шапкой),
 * Sidebar (навигационная панель) и Header (шапка страницы).
 */
export { AppShell } from "./app-shell";
export { Sidebar } from "./sidebar";
export { Header } from "./header";
