/**
 * Макет для маршрутов аутентификации (`/(auth)/*`).
 * Сквозной макет, который просто передаёт дочерние элементы без
 * дополнительной обёртки. Используется для страниц входа, регистрации,
 * восстановления и сброса пароля.
 *
 * @param children - Дочерние компоненты (страница аутентификации).
 * @returns Дочерние элементы без изменений.
 */
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
